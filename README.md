# CTFd Archive Generetor

## Demo

```
git clone git@github.com:ryotosaito/ctfd-archive-generator.git
cd ctfd-archive-generator
pip install .
ctfarchive -c example/config.yaml -o archive.zip
```

Then upload archive.zip to CTFd